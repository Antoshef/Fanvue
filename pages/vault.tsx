import styled from "@emotion/styled";
import { FC } from "react";

const Vault: FC = () => {

  const GridTable = styled("div")({
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap"
  })

  return (
    <GridTable>

    </GridTable>
  )
}

export default Vault